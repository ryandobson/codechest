% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova_multiverse.R
\name{anova_multi}
\alias{anova_multi}
\title{Mini-multiverse analysis for a one-way design}
\usage{
anova_multi(
  formula,
  data,
  contrast = NULL,
  decisions = NULL,
  conf = 0.95,
  trim = 0.2,
  alpha = 0.05
)
}
\arguments{
\item{formula}{A two-sided formula, \code{outcome ~ group}.}

\item{data}{A data frame.}

\item{contrast}{Length-2 character vector naming the two groups to contrast,
e.g. \code{c("high", "vehicle")}. Defaults to the last and first factor levels.}

\item{decisions}{Named list of decisions to cross. Defaults to
\code{transform = c("none","log","sqrt")}, \code{variance = c("pooled","welch")},
\code{estimator = c("ols","trim20")}.}

\item{conf}{Confidence level for the contrast intervals.}

\item{trim}{Trim proportion for the robust specifications. Default \code{.2}.}

\item{alpha}{Threshold used to judge whether specifications disagree.}
}
\value{
An object of class \code{anova_multiverse}, with \code{results} (one row per
specification), \code{baseline}, and \code{n_disagree}.
}
\description{
Crosses the small analysis decisions you could defensibly have made,
refits under each, and reports whether the conclusion survives. This
operationalises the sensitivity analysis Field and Wilcox (2017) argue
should accompany every frequentist analysis.
}
\details{
The forest plot is anchored on a \strong{focal contrast} between two groups,
standardized so that transformed and untransformed specifications share an
axis. The omnibus test appears in the results table.
}
\examples{
\dontrun{
mv <- anova_multi(weight ~ group, data = PlantGrowth,
                  contrast = c("trt2", "ctrl"))
mv
anova_multi_plot(mv)
}

}
\seealso{
\code{\link[=anova_ref]{anova_ref()}}, with \code{"robust_sensitivity"}, for the argument this
implements; \code{\link[=anova_multi_plot]{anova_multi_plot()}} for the forest plot.
}
