% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lavaan_block_parser.R
\name{.insert_global}
\alias{.insert_global}
\title{Insert lines that apply to the whole model, not to one block}
\usage{
.insert_global(model_string, parser, lines, where = c("prelude", "postlude"))
}
\arguments{
\item{model_string}{A lavaan model-syntax string.}

\item{parser}{One of \code{\link[=parse_single_block]{parse_single_block()}}, \code{\link[=parse_group_blocks]{parse_group_blocks()}}, or
\code{\link[=parse_level_blocks]{parse_level_blocks()}}.}

\item{lines}{A character vector of lines to insert.}

\item{where}{\code{"prelude"} (default) to prepend before the first block, or
\code{"postlude"} to append after the last block.}
}
\value{
The reassembled model string with \code{lines} inserted.
}
\description{
Adds \code{lines} either to the model's prelude (creating one if none exists)
or as a trailing "postlude" block. Used by the \verb{set_*()} functions to
attach model-wide constraints — e.g. the \code{l1 + l2 + l3 == 3} sum
constraint an effects-coded loading identification needs — which belong
once per model rather than once per group/level block.
}
\keyword{internal}
