% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova_describe.R
\name{anova_check_plots}
\alias{anova_check_plots}
\title{Diagnostic plots for an assumption check}
\usage{
anova_check_plots(
  x,
  bins = 12,
  theme = c("jeremy", "dark", "gridline", "none"),
  base_size = 14
)
}
\arguments{
\item{x}{An \code{anova_check} object, or a fitted \code{aov()}/\code{lm()}.}

\item{bins}{Bins for the residual histogram.}

\item{theme}{One of \code{"jeremy"} (default), \code{"dark"}, \code{"gridline"}, or
\code{"none"}; or any ggplot2 theme object or theme-returning function.}

\item{base_size}{Base font size passed to the theme. Default \code{14}.}
}
\value{
A named list of ggplots: \code{qq}, \code{residuals}, \code{boxplot}.
}
\description{
Q-Q plot of the residuals, a histogram of the residuals, and a boxplot of
the outcome by condition. Returned as a named list rather than drawn, so you
can print the one you want or arrange them however you like.
}
\details{
The Q-Q plot is the one to read when a normality test is significant. It
separates the two cases that matter and that a p value cannot distinguish:
a few extreme observations, versus genuine skew or heavy tails through the
whole distribution.
}
\examples{
\dontrun{
chk <- anova_check(weight ~ group, data = PlantGrowth)
p <- anova_check_plots(chk)
p$qq
}

}
\seealso{
\code{\link[=anova_check]{anova_check()}}, \code{\link[=anova_ref]{anova_ref()}} with \code{"normality"}.
}
