% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lavaan_syntax_setters.R
\name{set_item_covariances}
\alias{set_item_covariances}
\title{Set residual covariances between items}
\usage{
set_item_covariances(
  model_string,
  parser,
  items,
  item_covars,
  mode = c("equal_all", "equal_except", "free_all"),
  free_pairs = NULL,
  label_prefix = "c"
)
}
\arguments{
\item{model_string}{A lavaan model syntax string.}

\item{parser}{A block parser: \code{\link[=parse_single_block]{parse_single_block()}}, \code{\link[=parse_group_blocks]{parse_group_blocks()}},
or \code{\link[=parse_level_blocks]{parse_level_blocks()}}.}

\item{items}{Character vector of all items in scope. Any pre-existing
covariance among these is removed before the new lines are written.}

\item{item_covars}{A named character vector of item pairs, where names are
the left-hand items and values the right-hand items (e.g.
\code{c(x1 = "x2")} for a covariance between \code{x1} and \code{x2}).}

\item{mode}{One of \code{"equal_all"} (default), \code{"equal_except"}, or
\code{"free_all"}.}

\item{free_pairs}{Pairs to leave free when \code{mode = "equal_except"}.}

\item{label_prefix}{Prefix for generated parameter labels. Default \code{"c"}.}
}
\value{
The model string with item covariances rewritten.
}
\description{
Adds \verb{~~} covariance lines between named item pairs, with cross-block
equality labels according to \code{mode}. Use this for correlated residuals
between items that share method variance or wording.
}
\examples{
ms <- "f1 =~ x1 + x2 + x3"
cat(set_item_covariances(ms, parse_single_block,
                         items = c("x1", "x2", "x3"),
                         item_covars = c(x1 = "x2")))

}
\seealso{
Other lavaan syntax:
\code{\link[=build_model]{build_model()}},
\code{\link[=set_factor_covariances]{set_factor_covariances()}},
\code{\link[=set_intercepts]{set_intercepts()}},
\code{\link[=set_loadings]{set_loadings()}},
\code{\link[=set_means]{set_means()}},
\code{\link[=set_residuals]{set_residuals()}},
\code{\link[=set_variances]{set_variances()}}
}
\concept{lavaan syntax}
