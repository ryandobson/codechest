% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/renaming.R
\name{rename_lmers}
\alias{rename_lmers}
\title{Rename coefficient rows for a list of models}
\usage{
rename_lmers(model_list, rename_vec, data, model_path)
}
\arguments{
\item{model_list}{A list whose elements contain (at least) a fitted model object.}

\item{rename_vec}{Optional pre-existing mapping vector passed to \code{rename_lmer()}.}

\item{data}{The data frame used to fit the models (used to infer factor encodings).}

\item{model_path}{Character vector naming the path inside each list element at which
to find the fitted model (e.g., \code{c("mc","final_model")} or \code{c("mr")}).}
}
\value{
The input \code{model_list}, with a new element \code{$tidy_tbl} added
to each processed entry containing the renamed coefficient table.
}
\description{
Iterates over \code{model_list}, retrieves each fitted model via \code{model_path},
builds a tidy coefficient table with \code{rename_lmer()}, and stores it as
\code{$tidy_tbl} on each list element.
}
\examples{
\dontrun{
models_expanded <- rename_lmers(models_expanded, rename_vec = NULL,
                                data = df, model_path = c("mc","final_model"))
models_expanded[[1]]$tidy_tbl
}

}
\seealso{
\code{\link{rename_lmer}}, \code{\link{create_nice_names}}

Other renaming_helpers: 
\code{\link{create_nice_name}()},
\code{\link{create_nice_names}()},
\code{\link{rename_lmer}()}
}
\concept{renaming_helpers}
