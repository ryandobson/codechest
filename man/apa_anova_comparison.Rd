% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apa_helpers.R
\name{apa_anova_comparison}
\alias{apa_anova_comparison}
\title{Create an APA-Style Likelihood Ratio Test (Model Comparison) Table}
\usage{
apa_anova_comparison(
  anova_table,
  bold_title = "Table",
  italics_title = "Model Comparison (Likelihood Ratio Test)",
  table_note = "",
  font_size = 10,
  font = "Times New Roman",
  bold_best = TRUE,
  sig_level = FALSE
)
}
\arguments{
\item{anova_table}{A data frame, typically created by \code{\link[=anova]{anova()}} on a list of
nested models. Must contain model fit indices (e.g., AIC, BIC, logLik, Chisq, Df, Pr(>Chisq)).}

\item{bold_title}{A character string specifying the main table title (bolded).
Defaults to \code{"Table"}.}

\item{italics_title}{A character string specifying the secondary subtitle
(italicized). Defaults to \code{"Model Comparison (Likelihood Ratio Test)"}.}

\item{table_note}{Optional character string for a note appended to the footer.}

\item{font_size}{Numeric. Font size applied to all table elements.
Defaults to \code{10}.}

\item{font}{Character string specifying the font name (e.g., \code{"Times New Roman"}).}

\item{bold_best}{Logical. Whether to bold the best-fitting model
(smallest AIC or BIC). Defaults to \code{TRUE}.}

\item{sig_level}{Logical or numeric. If \code{FALSE}, no significance bolding is
applied. If numeric (e.g., \code{.05}), rows with p-values below this level are
bolded to indicate significant likelihood ratio tests. Defaults to \code{FALSE}.}
}
\value{
A flextable object (see \code{\link[flextable:flextable]{flextable::flextable()}} for table export options).
}
\description{
Formats a model comparison table (e.g., output from \code{\link[=anova]{anova()}}) into an
APA-style table using the \strong{flextable} and \strong{officer} packages.
The function supports automatic formatting of numerical columns,
bolding of significant model comparisons, and highlighting the best-fitting
model based on AIC or BIC.
}
\details{
This function is intended for use when comparing nested mixed or multilevel
models (e.g., from \code{\link[lme4:lmer]{lme4::lmer()}} or \code{\link[nlme:lme]{nlme::lme()}}) using likelihood ratio tests.
It automatically:
\itemize{
\item extracts key columns from the ANOVA comparison table,
\item reformats numeric and p-value columns to APA standards,
\item adds hierarchical bold/italic headers (title and subtitle),
\item bolds rows corresponding to significant likelihood ratio tests,
\item optionally bolds the best-fitting model (lowest AIC or BIC),
\item and returns a fully formatted \strong{flextable} object ready for
export to Word or PDF.
}
}
\examples{
\dontrun{
# Example with two nested lmer models
library(lme4)
m1 <- lmer(Reaction ~ 1 + (1 | Subject), sleepstudy)
m2 <- lmer(Reaction ~ Days + (1 | Subject), sleepstudy)
anova_tbl <- anova(m1, m2)
apa_anova_comparison(anova_tbl, bold_title = "Table 1", italics_title = "Model Comparison")
}

}
\seealso{
\code{\link[flextable:flextable]{flextable::flextable()}}, \code{\link[=anova]{anova()}}, \code{\link[lme4:lmer]{lme4::lmer()}}
}
