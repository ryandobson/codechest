% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apa_helpers.R
\name{save_apa_lmer_tables}
\alias{save_apa_lmer_tables}
\title{Save APA-Style LMER Tables to Image Files}
\usage{
save_apa_lmer_tables(
  model_list,
  directory,
  apa_table_path = "apa_table",
  prefix = "apa_table",
  create_subfolder = TRUE
)
}
\arguments{
\item{model_list}{A named list of model objects, each containing an
APA-formatted \strong{flextable} (e.g., \verb{$apa_table}).}

\item{directory}{Character string specifying the base directory where
image files will be saved. Created recursively if it does not exist.}

\item{apa_table_path}{Character string indicating the element name or nested
path to the APA table within each model list element.
Defaults to \code{"apa_table"}.}

\item{prefix}{Character string used as both the filename prefix and the
default subfolder name. Defaults to \code{"apa_table"}.}

\item{create_subfolder}{Logical or character.
\itemize{
\item If \code{TRUE} (default), a subfolder named after \code{prefix} is created.
\item If \code{FALSE}, tables are saved directly in \code{directory}.
\item If a character string is provided, it is used as a custom subfolder name.
}}
}
\value{
Invisibly returns the input \code{model_list} after saving all tables to disk.
A message is printed indicating the final output location.
}
\description{
Exports APA-formatted \strong{flextable} objects (created by \code{\link[=apa_lmer_model]{apa_lmer_model()}}
or \code{\link[=run_apa_lmer_model]{run_apa_lmer_model()}}) as \code{.png} image files.
Each model's table is saved to a user-specified output directory, with
optional automatic subfolder creation and file naming based on a prefix.
}
\details{
This function loops through a list of model specifications that each contain
an APA-style table (by default stored under the element name \code{"apa_table"}).
Each table is converted to a high-resolution image using
\code{\link[flextable:save_as_image]{flextable::save_as_image()}}, and written to disk with filenames that include
both the specified \code{prefix} and model name.

By default, images are saved to a new subfolder named after the \code{prefix}
(e.g., \code{"apa_table/"}). Users can disable subfolder creation or provide a
custom folder name via the \code{create_subfolder} argument.
}
\examples{
\dontrun{
# Generate APA-style tables first
models <- run_apa_lmer_model(model_list)

# Save tables as .png images in a subfolder
save_apa_lmer_tables(models, directory = "output/results")

# Save tables directly to the specified folder (no subfolder)
save_apa_lmer_tables(models, directory = "output/results", create_subfolder = FALSE)
}

}
\seealso{
\code{\link[=apa_lmer_model]{apa_lmer_model()}}, \code{\link[=run_apa_lmer_model]{run_apa_lmer_model()}}, \code{\link[flextable:save_as_image]{flextable::save_as_image()}}
}
