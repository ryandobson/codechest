% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fixed_effects_removal.R
\name{log_fe_int_drop}
\alias{log_fe_int_drop}
\title{Log a step in the fixed-effect interaction dropping workflow}
\usage{
log_fe_int_drop(
  history,
  step,
  type,
  model,
  fixed_effects,
  to_get_removed,
  mod_comp,
  name_prefix = NULL
)
}
\arguments{
\item{history}{A list used as the running history/log.}

\item{step}{Integer step number (e.g., 0 for the initial state).}

\item{type}{Character step label, e.g. \code{"initial_model"}, \code{"edited_model"},
\code{"final_model"}.}

\item{model}{The fitted model object at this step (e.g., \code{lmerMod}, \code{lm}).}

\item{fixed_effects}{Character vector of fixed-effect terms represented at this step.}

\item{to_get_removed}{Character vector of interaction terms queued to remove next,
or \code{NULL} if none.}

\item{mod_comp}{Optional model-comparison result (e.g., from \code{anova()}), or \code{NULL}.}

\item{name_prefix}{Optional character prefix for the history entry name; if \code{NULL},
names are generated like \code{"s00_initial_model"}.}
}
\value{
The updated \code{history} list with a new named entry.
}
\description{
Appends a named entry to a history list capturing the current step, the model,
the fixed effects considered, any terms queued for removal, and an optional
model-comparison object.
}
\examples{
\dontrun{
H <- list()
H <- log_fe_int_drop(H, step = 0, type = "initial_model",
                     model = NULL, fixed_effects = c("x1","x2","x1*x2"),
                     to_get_removed = "x1*x2", mod_comp = NULL)
names(H)
 }
}
\seealso{
\code{\link{fixed_effect_int_drops}}
}
\keyword{internal}
