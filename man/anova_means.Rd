% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova_describe.R
\name{anova_means}
\alias{anova_means}
\title{Group means with model-based standard errors and confidence intervals}
\usage{
anova_means(model, conf = 0.95, pooled = TRUE)
}
\arguments{
\item{model}{A fitted \code{aov()} or \code{lm()}.}

\item{conf}{Confidence level. Default \code{0.95}.}

\item{pooled}{Use the model's pooled error term. Default \code{TRUE}.}
}
\value{
A data.frame, one row per group, with \code{n}, \code{mean}, \code{sd}, \code{se},
\code{ci_low}, \code{ci_high}. The \code{se_type} attribute records which SE was used.
}
\description{
By default the standard error is the model-based one, \code{sqrt(MS_W / n_j)},
with the model's error degrees of freedom. That is the standard error
consistent with the F test being reported, so error bars built from it match
the model. Error bars built from each group's own SD do not.
}
\details{
\code{pooled = FALSE} gives each group its own SD and its own \code{n_j - 1} df, which
is three separate one-sample intervals drawn on shared axes. Use it when
homogeneity is violated, or when the figure is meant to describe each group
on its own terms without leaning on a model.
}
\examples{
fit <- aov(weight ~ group, data = PlantGrowth)
anova_means(fit)
anova_means(fit, pooled = FALSE)

}
\seealso{
\code{\link[=anova_ref]{anova_ref()}} with \code{"se_mean"} or \code{"ms_within"} for the reasoning.
}
