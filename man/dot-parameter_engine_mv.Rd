% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lavaan_constraint_engine.R
\name{.parameter_engine_mv}
\alias{.parameter_engine_mv}
\title{Decide a factor-level parameter's label (means and variances)}
\usage{
.parameter_engine_mv(
  mode,
  block_id,
  group_name,
  groups_to_fix = NULL,
  label_prefix,
  equal_label = "1"
)
}
\arguments{
\item{mode}{One of \code{"all_equal"}, \code{"first_zero"}/\code{"first_one"},
\code{"groups_zero"}/\code{"groups_one"}, \code{"all_zero"}/\code{"all_one"}, or \code{"none"}.
The \verb{_zero} variants are for means, \verb{_one} for variances.}

\item{block_id}{The current block's (group's) 1-based id.}

\item{group_name}{The current block's group name, used to check
\code{groups_to_fix} when it's supplied as group names rather than ids.}

\item{groups_to_fix}{Optional group ids or names to fix under
\code{mode = "groups_zero"}/\code{"groups_one"}.}

\item{label_prefix}{The label prefix (e.g. \code{"m"}, \code{"v"}).}

\item{equal_label}{The label suffix used under \code{mode = "all_equal"}
(default \code{"1"}, giving e.g. \code{"m1"}).}
}
\value{
A list with \code{fixed} (logical), \code{fixed_value} (0 or 1, when
\code{fixed}), \code{label} (a label string, or \code{NULL}), and \code{free} (logical).
}
\description{
The label engine behind latent means and variances: given a \code{mode},
decides whether the factor's mean/variance should be fixed to an
identifying constant (0 for means, 1 for variances), given a shared
label across all blocks, or freely estimated per block.
}
\keyword{internal}
