% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_formulas.R
\name{build_random_term}
\alias{build_random_term}
\title{Build Random Effects Term for Mixed Models}
\usage{
build_random_term(
  random_effects,
  group,
  covary = TRUE,
  intercept = TRUE,
  data = NULL
)
}
\arguments{
\item{random_effects}{Character vector of random effect variable names.
Can include \code{"1"} to specify a random intercept; if not present and \code{intercept = TRUE},
one will be added automatically.}

\item{group}{Character scalar giving the grouping variable for the random effects
(e.g., \code{"person_id"}, \code{"family_id"}).}

\item{covary}{Logical; whether to model correlated (\code{TRUE}) or uncorrelated (\code{FALSE})
random slopes and intercepts.}

\item{intercept}{Logical; if \code{TRUE}, automatically add a random intercept when not specified.}

\item{data}{Optional data frame. If supplied, variable existence will be checked.}
}
\value{
A character string representing the random effects portion of a mixed model formula,
e.g. \code{"(1 + slope | group)"} or \code{"(1 + slope || group)"}.
}
\description{
Constructs a properly formatted random effects term for use in mixed-effects model formulas.
Automatically checks variable existence (if data provided), adds intercepts if missing,
and allows specification of correlated (\code{|}) or uncorrelated (\code{||}) random slopes.
}
\examples{
build_random_term(c("1", "x"), group = "id")
build_random_term("x", group = "id", covary = FALSE)

}
