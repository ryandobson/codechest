% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lavaan_syntax_setters.R
\name{set_loadings}
\alias{set_loadings}
\title{Set factor loadings across blocks}
\usage{
set_loadings(
  model_string,
  parser,
  factor_name,
  items,
  mode = c("equal_all", "equal_except", "free_all"),
  free_items = NULL,
  scale_id = c("none", "marker", "effects_coded"),
  marker_items = NULL,
  label_prefix = "l"
)
}
\arguments{
\item{model_string}{A lavaan model syntax string.}

\item{parser}{A block parser: \code{\link[=parse_single_block]{parse_single_block()}}, \code{\link[=parse_group_blocks]{parse_group_blocks()}},
or \code{\link[=parse_level_blocks]{parse_level_blocks()}}.}

\item{factor_name}{Name of the latent factor whose loadings to set.}

\item{items}{Character vector of item names loading on \code{factor_name}.}

\item{mode}{One of \code{"equal_all"} (default), \code{"equal_except"}, or
\code{"free_all"}.}

\item{free_items}{Items to leave free when \code{mode = "equal_except"}.}

\item{scale_id}{Scale identification: \code{"none"} (default), \code{"marker"} (fix a
marker item's loading to 1), or \code{"effects_coded"} (constrain loadings to
sum to the number of items).}

\item{marker_items}{Named list, keyed by factor, giving the marker item for
each. Required when \code{scale_id = "marker"}.}

\item{label_prefix}{Prefix for generated parameter labels. Default \code{"l"}.}
}
\value{
The model string with loadings rewritten.
}
\description{
Rewrites the \verb{=~} loading line for one factor in every block of a model
string, applying cross-block equality labels according to \code{mode}. This is
the workhorse for metric (loading) invariance: \code{mode = "equal_all"} labels
each item's loading identically across groups, \code{"equal_except"} frees the
items named in \code{free_items}, and \code{"free_all"} leaves every loading free.
}
\examples{
ms <- "f1 =~ x1 + x2 + x3"
cat(set_loadings(ms, parse_single_block, "f1", c("x1", "x2", "x3")))

}
\seealso{
\code{\link[=build_model]{build_model()}}, which calls this as part of a full model.

Other lavaan syntax:
\code{\link[=build_model]{build_model()}},
\code{\link[=set_factor_covariances]{set_factor_covariances()}},
\code{\link[=set_intercepts]{set_intercepts()}},
\code{\link[=set_item_covariances]{set_item_covariances()}},
\code{\link[=set_means]{set_means()}},
\code{\link[=set_residuals]{set_residuals()}},
\code{\link[=set_variances]{set_variances()}}
}
\concept{lavaan syntax}
