% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interaction_helpers.R
\name{tidy_emtrends}
\alias{tidy_emtrends}
\title{Batch Extraction of Simple Slopes Across Multiple Models and Variables}
\usage{
tidy_emtrends(model_list, specs, vars, model_path)
}
\arguments{
\item{model_list}{A named list of models (or model containers), each containing
a fitted mixed-effects model object under the element specified by \code{model_path}.}

\item{specs}{Character string naming the moderator variable (passed to
\code{\link[=tidy_emtrend]{tidy_emtrend()}}).}

\item{vars}{Character vector of focal predictor variable names whose simple
slopes should be estimated.}

\item{model_path}{Character string indicating where to find the model object
within each list element (e.g., \code{"model"} or \code{"post_mlm$model"}).}
}
\value{
A tidy data frame combining simple slope estimates across all specified models
and variables. The resulting object includes the following columns:
\code{model}, \code{variable}, \code{moderator_value}, \code{slope}, \code{SE}, \code{df},
\code{lower_ci}, \code{upper_ci}, and \code{p.value}.
Returns \code{NULL} if no valid results are obtained.
}
\description{
Applies \code{\link[=tidy_emtrend]{tidy_emtrend()}} iteratively across a list of models and focal
variables, producing a combined tidy data frame of all estimated simple slopes.
This allows for automated extraction of interaction probes across multiple
models in a standardized format.
}
\details{
For each model in \code{model_list}, the function retrieves the model object from
the element specified by \code{model_path}, then computes simple slopes for each
focal variable in \code{vars} using \code{\link[=tidy_emtrend]{tidy_emtrend()}}.
Results from all models and variables are combined into a single tidy data
frame suitable for plotting or meta-analytic summary.
}
\examples{
\dontrun{
models <- list(
  M1 = list(post_mlm = list(model = m1)),
  M2 = list(post_mlm = list(model = m2))
)

tidy_emtrends(
  model_list = models,
  specs = "Moderator",
  vars = c("Predictor1", "Predictor2"),
  model_path = "post_mlm$model"
)
}

}
\seealso{
\code{\link[=tidy_emtrend]{tidy_emtrend()}}, \code{\link[emmeans:emtrends]{emmeans::emtrends()}}
}
