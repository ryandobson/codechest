% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apa_tbl_helpers.R
\name{run_apa_fed_report}
\alias{run_apa_fed_report}
\title{Generate and Attach APA-Style Fixed-Effect Reduction Reports}
\usage{
run_apa_fed_report(
  model_list,
  fed_path = "fed",
  title_prefix = "Fixed-Effect Reduction Process: ",
  font = "Times New Roman",
  font_size = 11
)
}
\arguments{
\item{model_list}{A named list of model objects, where each element may contain
a fixed-effect reduction history object (e.g., produced by
\code{\link[=fixed_effect_drops]{fixed_effect_drops()}} or \code{mlm_comparison()}).}

\item{fed_path}{Character string specifying the nested path (using \code{$} notation)
to the fixed-effect reduction history object within each model element.
Defaults to \code{"fed"}.}

\item{title_prefix}{Character string prefix applied to the title of each
generated report. Defaults to \code{"Fixed-Effect Reduction Process: "}.}

\item{font}{Character string specifying the font family for the flextable.
Defaults to \code{"Times New Roman"}.}

\item{font_size}{Numeric value indicating the font size used in the report.
Defaults to \code{11}.}
}
\value{
A list of models identical to the input \code{model_list} but with an additional
\verb{$apa_fed_report} element appended to each model that successfully generated
a table.
}
\description{
This function iterates over a list of model objects (e.g., multilevel models)
and generates an APA-formatted summary of the fixed-effect reduction process
for each model using \code{\link[=apa_fed_report]{apa_fed_report()}}. The resulting flextable reports are
stored within each model entry in the list for later export or display.
}
\details{
The function extracts each model’s fixed-effect reduction history using
a safe accessor that supports nested list paths (e.g., \code{"results$fed"}),
generates the APA-style table using \code{\link[=apa_fed_report]{apa_fed_report()}}, and attaches the
resulting flextable as a new element (\verb{$apa_fed_report}) to each model
entry in the list.
}
\examples{
\dontrun{
models <- run_apa_fed_report(model_list = my_models, fed_path = "fed")
models$Model1$apa_fed_report
}

}
\seealso{
\code{\link[=apa_fed_report]{apa_fed_report()}}, \code{\link[=save_apa_fed_reports]{save_apa_fed_reports()}}
}
