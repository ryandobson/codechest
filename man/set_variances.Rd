% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lavaan_syntax_setters.R
\name{set_variances}
\alias{set_variances}
\title{Set a factor's variance across blocks}
\usage{
set_variances(
  model_string,
  parser,
  factor_name,
  variances_mode = c("first_one", "all_one", "groups_one", "none", "all_equal"),
  groups_to_fix = NULL,
  label_prefix = "v"
)
}
\arguments{
\item{model_string}{A lavaan model syntax string.}

\item{parser}{A block parser: \code{\link[=parse_single_block]{parse_single_block()}}, \code{\link[=parse_group_blocks]{parse_group_blocks()}},
or \code{\link[=parse_level_blocks]{parse_level_blocks()}}.}

\item{factor_name}{Name of the latent factor whose loadings to set.}

\item{variances_mode}{One of \code{"first_one"} (fix the first block's variance
to 1), \code{"all_one"}, \code{"groups_one"} (fix only the blocks named in
\code{groups_to_fix}), \code{"none"}, or \code{"all_equal"}.}

\item{groups_to_fix}{Character vector of group names to fix, used when
\code{variances_mode = "groups_one"}.}

\item{label_prefix}{Prefix for generated parameter labels. Default \code{"v"}.}
}
\value{
The model string with factor variance lines rewritten.
}
\description{
Writes the \code{factor ~~ factor} variance line in every block. Fixing the
variance to 1 in one or all blocks is one of the two standard ways to give
a latent variable a scale (the other being a marker item, see
\code{\link[=set_loadings]{set_loadings()}}).
}
\details{
This function carries a known limitation inherited from the original
implementation, and emits a warning on every call to say so:
\code{.remove_factor_variances()} matches on \verb{factor ~~} and so will also strip
\emph{covariance} lines involving that factor, not just its variance. If you
are setting both factor variances and factor covariances, set the
variances first. The warning is deliberate — it documents real current
behavior rather than a hypothetical risk, and fixing the underlying regex
needs a battery of invariance models to validate against before it can be
trusted.
}
\examples{
ms <- "f1 =~ x1 + x2 + x3"
cat(suppressWarnings(set_variances(ms, parse_single_block, "f1")))

}
\seealso{
Other lavaan syntax:
\code{\link[=build_model]{build_model()}},
\code{\link[=set_factor_covariances]{set_factor_covariances()}},
\code{\link[=set_intercepts]{set_intercepts()}},
\code{\link[=set_item_covariances]{set_item_covariances()}},
\code{\link[=set_loadings]{set_loadings()}},
\code{\link[=set_means]{set_means()}},
\code{\link[=set_residuals]{set_residuals()}}
}
\concept{lavaan syntax}
