% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cor_information.R
\name{pub_cors_all}
\alias{pub_cors_all}
\title{Single-group (pooled) correlation matrix with publication labels}
\usage{
pub_cors_all(
  data,
  cor_vars,
  rename_vec = NULL,
  rename_cols = TRUE,
  cor_use = "pairwise",
  rnd = 2
)
}
\arguments{
\item{data}{A data frame.}

\item{cor_vars}{Character vector of variable names to include.}

\item{rename_vec}{Optional named character vector for renaming variables
(robust to old→new vs. new→old mapping); see \code{\link[=rename_rnd_cor]{rename_rnd_cor()}}.}

\item{rename_cols}{Logical; if \code{TRUE}, row names become
\code{"<original> (1..n)"} and column names become \code{"1..n"}.}

\item{cor_use}{Passed to \code{\link[stats:cor]{stats::cor()}} \code{use=} argument.}

\item{rnd}{Integer number of decimals to round to. Set \code{FALSE} to skip.}
}
\value{
A correlation matrix (numeric) with possibly renamed and numbered
dimnames suitable for publication.
}
\description{
Compute a correlation matrix over \code{cor_vars}, optionally rename and
round it, and optionally add publication-style numbering.
}
\examples{
set.seed(1)
df <- data.frame(A = rnorm(40), B = rnorm(40), C = rnorm(40))
pub_cors_all(df, c("A","B","C"),
             rename_vec = c(A = "Alpha", B = "Beta", C = "Gamma"),
             rename_cols = TRUE, rnd = 2)

}
\seealso{
\code{\link[stats:cor]{stats::cor()}}, \code{\link[=rename_rnd_cor]{rename_rnd_cor()}}
}
