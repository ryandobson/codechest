% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lavaan_block_parser.R
\name{.map_blocks}
\alias{.map_blocks}
\title{Rewrite every block of a model string}
\usage{
.map_blocks(model_string, parser, FUN)
}
\arguments{
\item{model_string}{A lavaan model-syntax string.}

\item{parser}{One of \code{\link[=parse_single_block]{parse_single_block()}}, \code{\link[=parse_group_blocks]{parse_group_blocks()}}, or
\code{\link[=parse_level_blocks]{parse_level_blocks()}} (unquoted function, not a string).}

\item{FUN}{A function of \verb{(block, indices)} returning the rewritten
character vector of lines for that block. \code{block} is the block's lines;
\code{indices} is its metadata list (see the \verb{parse_*} functions).}
}
\value{
The reassembled model string.
}
\description{
Parses \code{model_string} with \code{parser}, applies \code{FUN} to each block's lines
in turn, and reassembles the result. This is the core engine every
\verb{set_*()} function in lavaan_syntax_setters.R is built on: they each
supply a \code{FUN} that strips out one kind of parameter line (loadings,
intercepts, ...) and writes new ones for that block.
}
\keyword{internal}
