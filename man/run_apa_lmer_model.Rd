% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apa_tbl_helpers.R
\name{run_apa_lmer_model}
\alias{run_apa_lmer_model}
\title{Generate APA-Style Tables for Multiple Linear Mixed-Effects Models}
\usage{
run_apa_lmer_model(
  model_list,
  data = NULL,
  data_path = "data",
  nice_names = NULL,
  model_path = NULL,
  bold_title = "Table",
  italics_title = "",
  table_note = "",
  font_size = 10,
  font = "Times New Roman",
  effects_to_bold = NULL,
  sig_level = 0.05,
  reorder_predictors = TRUE
)
}
\arguments{
\item{model_list}{A named list of model specifications, each containing a
model comparison object (\verb{$mc_menses}) from which the final model can be
extracted via \code{\link[=final_model_fed]{final_model_fed()}}.}

\item{data}{Optional data frame used for all models. If \code{NULL}, each model’s
dataset is retrieved via \code{data_path}.}

\item{data_path}{Character string specifying the nested path to the data
element within each model entry (e.g., \code{"data"}). Defaults to \code{"data"}.}

\item{nice_names}{Optional named vector mapping original variable names to
user-friendly labels (passed to \code{\link[=apa_lmer_model]{apa_lmer_model()}}).}

\item{model_path}{Character string specifying the name of the element
within each model object that contains the APA flextable report.
Defaults to \code{"NULL"}.}

\item{bold_title}{Character string specifying the base title (bolded in the table).
A numeric index is appended automatically. Defaults to \code{"Table"}.}

\item{italics_title}{Character string specifying the italicized subtitle.
Typically includes the model name or formula.}

\item{table_note}{Optional character string for a note displayed at the table footer.}

\item{font_size}{Numeric font size applied to all text. Defaults to \code{10}.}

\item{font}{Character string specifying the font family (e.g., \code{"Times New Roman"}).}

\item{effects_to_bold}{Optional character vector of predictor names or
interaction terms to bold manually in all tables.}

\item{sig_level}{Numeric significance threshold for automatic bolding of
significant effects. Defaults to \code{0.05}.}

\item{reorder_predictors}{Logical; if \code{TRUE} (default), predictors are ordered
by interaction complexity (main effects first, then interactions).}
}
\value{
A list identical in structure to \code{model_list}, but with an additional
element \verb{$apa_table} appended to each model entry.
Each \verb{$apa_table} is a formatted \strong{flextable} returned by \code{\link[=apa_lmer_model]{apa_lmer_model()}}.
}
\description{
Iterates over a list of model specifications, extracts the final fitted model
from each (via \code{\link[=final_model_fed]{final_model_fed()}}), and produces APA-style \strong{flextable}
outputs using \code{\link[=apa_lmer_model]{apa_lmer_model()}}.
Each formatted table is added back into the model list under the element
\verb{$apa_table}.
}
\details{
This function is a convenience wrapper for batch-generating publication-ready
tables of fixed effects from multiple \code{lmerTest::summary()} models.
It automatically handles numbering of table titles, retrieval of model data,
and consistent formatting options (font, note, and significance bolding).

The user may specify a global data frame (\code{data}) shared across models or,
alternatively, a data path within each model list entry (\code{data_path}).
If a global data frame is not provided, the function attempts to locate the
dataset by evaluating the object name specified at \code{data_path}.
}
\examples{
\dontrun{
library(lme4)
# Example model list (simplified)
model_list <- list(
  Model1 = list(name = "Reaction ~ Days", data = "sleepstudy", mc_menses = mc_obj1),
  Model2 = list(name = "Reaction ~ Days + (1|Subject)", data = "sleepstudy", mc_menses = mc_obj2)
)

# Generate APA-formatted tables for each model
results <- run_apa_lmer_model(model_list)

# Access the first formatted table
results$Model1$apa_table
}

}
\seealso{
\code{\link[=apa_lmer_model]{apa_lmer_model()}}, \code{\link[=final_model_fed]{final_model_fed()}}, \code{\link[flextable:flextable]{flextable::flextable()}}
}
