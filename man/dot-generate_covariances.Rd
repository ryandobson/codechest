% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lavaan_constraint_engine.R
\name{.generate_covariances}
\alias{.generate_covariances}
\title{Generate covariance lines for a set of variable pairs}
\usage{
.generate_covariances(
  covars,
  mode,
  block_id,
  free_pairs,
  label_prefix,
  explicit_labels_when_free = TRUE
)
}
\arguments{
\item{covars}{A data frame with \code{lhs} and \code{rhs} character columns (one
row per pair), as produced by \code{\link[=.make_factor_pairs]{.make_factor_pairs()}} or supplied
directly for item covariances.}

\item{mode}{One of \code{"free_all"}, \code{"equal_all"}, \code{"equal_except"},
\code{"fixed_zero"}.}

\item{block_id}{The current block's id, or \code{NULL}.}

\item{free_pairs}{Pair ids (\code{"lhs_rhs"}) to free under
\code{mode = "equal_except"}.}

\item{label_prefix}{The covariance label prefix (e.g. \code{"c"}, \code{"phi"}).}

\item{explicit_labels_when_free}{See \code{\link[=.parameter_label_engine]{.parameter_label_engine()}}.}
}
\value{
A character vector of covariance lines, one per row of \code{covars}.
}
\description{
Shared by \code{\link[=set_item_covariances]{set_item_covariances()}} and \code{\link[=set_factor_covariances]{set_factor_covariances()}}: given
a data frame of \code{lhs}/\code{rhs} pairs, generates one \verb{lhs ~~ [label*]rhs}
line per pair.
}
\keyword{internal}
