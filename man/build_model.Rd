% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_model.R
\name{build_model}
\alias{build_model}
\title{Build a lavaan model string with invariance constraints}
\usage{
build_model(
  factors,
  type = c("single", "groups", "levels", "factors"),
  groups = NULL,
  levels = c(1, 2),
  variances_mode = c("first_one", "all_one", "groups_one", "none", "all_equal"),
  means_mode = c("first_zero", "all_zero", "groups_zero", "none", "all_equal"),
  var_groups_to_fix = NULL,
  mean_groups_to_fix = NULL,
  loadings_mode = c("equal_all", "equal_except", "free_all"),
  intercepts_mode = c("equal_all", "equal_except", "free_all"),
  residuals_mode = c("equal_all", "equal_except", "free_all"),
  item_covars_mode = c("equal_all", "equal_except", "free_all"),
  factor_covars_mode = c("free_all", "equal_all", "equal_except", "fixed_zero"),
  factor_free_pairs = NULL,
  factor_ids = NULL,
  free_loadings = NULL,
  free_intercepts = NULL,
  free_residuals = NULL,
  item_covars = NULL,
  free_item_covars = NULL,
  loadings_scale_id = c("none", "marker", "effects_coded"),
  loadings_marker_items = NULL,
  intercepts_scale_id = c("none", "marker", "effects_coded"),
  intercepts_marker_items = NULL,
  label_prefixes = list(loadings = "l", intercepts = "i", residuals = "r", variances =
    "v", means = "m", item_covs = "c", factor_covs = "phi"),
  verbose = FALSE
)
}
\arguments{
\item{factors}{A named list mapping each factor name to a character vector
of its items, e.g. \code{list(f1 = c("x1", "x2", "x3"))}.}

\item{type}{Model structure: \code{"single"} (one block), \code{"groups"}
(multi-group, requires \code{groups}), or \code{"levels"} (multilevel). The value
\code{"factors"} is accepted by the argument matcher but is not implemented
and will error — it is reserved for a planned cross-factor constraint
mode.}

\item{groups}{Character vector of group names. Required when
\code{type = "groups"}.}

\item{levels}{Numeric vector of level ids for \code{type = "levels"}.
Default \code{c(1, 2)}.}

\item{variances_mode}{Latent variance identification, per factor. One of
\code{"first_one"}, \code{"all_one"}, \code{"groups_one"}, \code{"none"}, \code{"all_equal"}.}

\item{means_mode}{Latent mean identification, per factor. One of
\code{"first_zero"}, \code{"all_zero"}, \code{"groups_zero"}, \code{"none"}, \code{"all_equal"}.}

\item{var_groups_to_fix}{Groups whose latent variance to fix, used with
\code{variances_mode = "groups_one"}.}

\item{mean_groups_to_fix}{Groups whose latent mean to fix, used with
\code{means_mode = "groups_zero"}.}

\item{loadings_mode}{Loading constraint, per factor. One of \code{"equal_all"},
\code{"equal_except"}, \code{"free_all"}.}

\item{intercepts_mode}{Intercept constraint for the whole model.}

\item{residuals_mode}{Residual variance constraint for the whole model.}

\item{item_covars_mode}{Constraint applied to item covariances.}

\item{factor_covars_mode}{Constraint applied to factor covariances. One of
\code{"free_all"}, \code{"equal_all"}, \code{"equal_except"}, \code{"fixed_zero"}.}

\item{factor_free_pairs}{Factor pairs left free when
\code{factor_covars_mode = "equal_except"}.}

\item{factor_ids}{Character vector of label prefixes, one per factor.
Generated automatically when \code{NULL}.}

\item{free_loadings, free_intercepts, free_residuals}{Items to leave free
under the corresponding \code{"equal_except"} mode.}

\item{item_covars}{Named character vector of item pairs to covary, e.g.
\code{c(x1 = "x2")}.}

\item{free_item_covars}{Item pairs left free when
\code{item_covars_mode = "equal_except"}.}

\item{loadings_scale_id}{Loading scale identification, per factor:
\code{"none"}, \code{"marker"}, or \code{"effects_coded"}.}

\item{loadings_marker_items}{Named list of marker items keyed by factor,
required when \code{loadings_scale_id = "marker"}.}

\item{intercepts_scale_id}{Intercept scale identification: \code{"none"},
\code{"marker"}, or \code{"effects_coded"}.}

\item{intercepts_marker_items}{Named list of intercept marker items.}

\item{label_prefixes}{Named list of label prefixes for each parameter type.}

\item{verbose}{Print a summary of the constraints as they are applied.}
}
\value{
A lavaan model syntax string.
}
\description{
Assembles a complete lavaan model syntax string from a list of factors and
their items, applying equality constraints across groups or levels. This is
the top-level entry point of the syntax toolkit: it calls \code{\link[=set_loadings]{set_loadings()}},
\code{\link[=set_intercepts]{set_intercepts()}}, \code{\link[=set_residuals]{set_residuals()}}, \code{\link[=set_variances]{set_variances()}}, \code{\link[=set_means]{set_means()}},
\code{\link[=set_item_covariances]{set_item_covariances()}}, and \code{\link[=set_factor_covariances]{set_factor_covariances()}} in the right order
and hands back a string ready for \code{lavaan::cfa()}.
}
\details{
The \verb{*_mode} arguments accept either a single value applied to every factor,
or a factor-keyed named list for per-factor control. That is what makes
partial invariance expressible: you can hold one factor's loadings equal
while freeing another's.

Each factor's parameter labels are prefixed with a factor id (\code{f1_}, \code{f2_},
...) so labels never collide across factors. Supply \code{factor_ids} to control
those prefixes yourself.

Items that load on more than one factor get their intercept and residual
written only once, when the item is first encountered. An item has a single
intercept and a single residual regardless of how many factors it loads on,
so writing them per factor would produce duplicate lines that lavaan
rejects.
}
\examples{
# a one-factor model in a single block
ms <- build_model(factors = list(f1 = c("x1", "x2", "x3")))
cat(ms)

# the same model across two groups, loadings held equal
mg <- build_model(
  factors = list(f1 = c("x1", "x2", "x3")),
  type    = "groups",
  groups  = c("a", "b")
)
cat(mg)

}
\seealso{
\code{\link[=set_loadings]{set_loadings()}} and the other setters this composes.

Other lavaan syntax:
\code{\link[=set_factor_covariances]{set_factor_covariances()}},
\code{\link[=set_intercepts]{set_intercepts()}},
\code{\link[=set_item_covariances]{set_item_covariances()}},
\code{\link[=set_loadings]{set_loadings()}},
\code{\link[=set_means]{set_means()}},
\code{\link[=set_residuals]{set_residuals()}},
\code{\link[=set_variances]{set_variances()}}
}
\concept{lavaan syntax}
