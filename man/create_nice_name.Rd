% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/renaming.R
\name{create_nice_name}
\alias{create_nice_name}
\title{Create a mapping for a single variable's model-encoded name}
\usage{
create_nice_name(data, var)
}
\arguments{
\item{data}{A data frame containing \code{var}.}

\item{var}{Character scalar; the variable name in \code{data}.}
}
\value{
A named character vector of length 1, e.g. \code{c(study = "study1")}.
For non-factors, this is \code{c(var = "var")}; for factors, the value
reflects how the term appears in the coefficient table given the contrasts.
}
\description{
Builds a one-element named character vector mapping the original variable
name (the \emph{name} of the element) to the coefficient-name used by
\code{lmer}/\code{glmer} in model output (the \emph{value}). Handles
continuous variables and factors (with or without contrasts).
}
\examples{
\dontrun{
create_nice_name(df, "study")   # e.g., c(study = "study1")
create_nice_name(df, "age")     # c(age = "age")
}

}
\seealso{
Other renaming_helpers: 
\code{\link{create_nice_names}()},
\code{\link{rename_lmer}()},
\code{\link{rename_lmers}()}
}
\concept{renaming_helpers}
