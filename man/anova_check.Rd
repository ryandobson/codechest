% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anova_assumptions.R
\name{anova_check}
\alias{anova_check}
\title{Check the assumptions of an ANOVA model}
\usage{
anova_check(
  object,
  data = NULL,
  id = NULL,
  design = c("auto", "oneway_bs", "oneway_ws"),
  center = c("mean", "median"),
  alpha = 0.05
)
}
\arguments{
\item{object}{A fitted \code{aov()} or \code{lm()}, or a two-sided formula.}

\item{data}{A data frame. Required when \code{object} is a formula.}

\item{id}{Name of the subject identifier column. Supplying it is what makes
the design within-subjects. Requires the formula interface.}

\item{design}{\code{"auto"} infers the design. \code{"oneway_bs"} and \code{"oneway_ws"}
force it.}

\item{center}{Centre for Levene's test: \code{"mean"} (classic Levene) or
\code{"median"} (Brown-Forsythe, more robust to skew).}

\item{alpha}{Threshold for the printed verdicts. Default \code{.05}.}
}
\value{
An object of class \code{anova_check}.
}
\description{
Runs the assumption checks appropriate to the design and recommends a next
step with a citation. Supports one-way between-subjects and one-way
within-subjects (repeated measures) designs.
}
\details{
Normality is tested on the model's \strong{residuals}, not the raw scores. The
raw values pool conditions with different means and can look non-normal
when every condition is perfectly normal.

\strong{Within-subjects designs take long-format data}, one row per subject per
condition, which is the format R wants everywhere else too. Pass the subject
identifier as \code{id} and nothing needs reshaping; the wide matrix needed for
the sphericity test is built internally. Subjects missing any condition are
dropped, and the count is reported.
}
\examples{
anova_check(aov(weight ~ group, data = PlantGrowth))
anova_check(weight ~ group, data = PlantGrowth)

\dontrun{
# within-subjects, long format
anova_check(rt ~ condition, data = d, id = "participant")
}

}
\seealso{
\code{\link[=anova_ref]{anova_ref()}} for the reasoning behind any concept cited in the
output, \code{\link[=anova_check_plots]{anova_check_plots()}} for the diagnostic plots.
}
