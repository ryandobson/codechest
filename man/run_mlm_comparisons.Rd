% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlm_comparison.R
\name{run_mlm_comparisons}
\alias{run_mlm_comparisons}
\title{Run Stepwise MLM Comparisons Across a List of Model Specifications}
\usage{
run_mlm_comparisons(model_list, model_env = NULL, data = NULL, verbose = FALSE)
}
\arguments{
\item{model_list}{A named list of model specifications.
Each element must include the components described above.}

\item{model_env}{An optional environment in which model formulas are built
and evaluated. If \code{NULL}, a new environment is created with
\code{parent = globalenv()}.}

\item{data}{Optional data frame. If \code{NULL}, each model entry’s
\verb{$data} element is retrieved by name using \code{\link[base:get]{base::get()}}.}

\item{verbose}{Logical. If \code{TRUE}, prints detailed messages from
\code{\link[=mlm_comparison]{mlm_comparison()}} as it runs. Defaults to \code{FALSE}.}
}
\value{
A list identical in structure to the input \code{model_list}, but with an
additional component \verb{$mc} appended to each model specification.
The \verb{$mc} element stores the model comparison history returned by
\code{\link[=mlm_comparison]{mlm_comparison()}}.
}
\description{
Iterates over a list of model specifications and performs a
stepwise random-effects comparison for each using \code{\link[=mlm_comparison]{mlm_comparison()}}.
This function automates batch evaluation of multiple dependent variables
or modeling setups, appending a model comparison history (\verb{$mc}) to each
entry in the provided list.
}
\details{
Each element of \code{model_list} should be a named list containing, at minimum:
\itemize{
\item \code{name}: a descriptive label for the model.
\item \code{data}: the data frame name or object containing the model variables.
\item \code{fixed_effects}: a character vector of fixed-effect predictors.
\item \code{random_effects}: a character vector of random-effect terms (e.g., \code{c("1", "slope1")}).
\item \code{grouping_variable}: the grouping factor for the multilevel model.
\item \code{dependent_variable}: the dependent variable name.
}

The function automatically creates a new environment if none is provided via
\code{model_env}. Each model’s comparison is run using \code{\link[=mlm_comparison]{mlm_comparison()}},
and the resulting comparison history is stored under the \verb{$mc} element.

Models without specified random effects are skipped with a message.
}
\examples{
\dontrun{
# Define two example model specifications
model_list <- list(
  ModelA = list(
    name = "Reaction ~ Days",
    data = "sleepstudy",
    dependent_variable = "Reaction",
    fixed_effects = "Days",
    random_effects = c("1", "Days"),
    grouping_variable = "Subject"
  ),
  ModelB = list(
    name = "Reaction ~ 1",
    data = "sleepstudy",
    dependent_variable = "Reaction",
    fixed_effects = "1",
    random_effects = "1",
    grouping_variable = "Subject"
  )
)

# Run comparisons for both
results <- run_mlm_comparisons(model_list, verbose = TRUE)

# Access the comparison history for the first model
results$ModelA$mc
}

}
\seealso{
\code{\link[=mlm_comparison]{mlm_comparison()}}, \code{\link[=append_step_mlm_comparison]{append_step_mlm_comparison()}}
}
