% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apa_helpers.R
\name{run_apa_mlm_report}
\alias{run_apa_mlm_report}
\title{Generate APA-Style Summary Tables for Multiple MLM Comparisons}
\usage{
run_apa_mlm_report(
  model_list,
  mc_path = "mc",
  bold_title = "",
  title_prefix = "Mixed Model Random-Effects Comparison: ",
  font = "Times New Roman",
  font_size = 11,
  sig_level = 0.2,
  verbosity = c("long", "short")
)
}
\arguments{
\item{model_list}{A named list of model specifications, each containing a
comparison history object (typically \verb{$mc}) created by \code{\link[=mlm_comparison]{mlm_comparison()}}.}

\item{mc_path}{Character string specifying the nested path to the MLM
comparison object within each list element (e.g., \code{"mc"} or \code{"results$mc"}).
Defaults to \code{"mc"}.}

\item{bold_title}{Character string specifying the bold header line for all
APA tables (e.g., \code{"Table"}). Defaults to an empty string.}

\item{title_prefix}{Character string prepended to each model name to create
the italicized subtitle (e.g.,
\code{"Mixed Model Random-Effects Comparison: "}). Defaults to that phrase.}

\item{font}{Character string specifying the font family to use throughout
the tables. Defaults to \code{"Times New Roman"}.}

\item{font_size}{Numeric font size applied to all text. Defaults to \code{11}.}

\item{sig_level}{Numeric significance threshold passed to
\code{\link[=apa_anova_comparison]{apa_anova_comparison()}} for bolding p-values. Defaults to \code{0.2}.}

\item{verbosity}{Character string indicating the level of detail in the
reports. Must be \code{"long"} (summary + detailed ANOVA tables) or \code{"short"}
(summary table only). Defaults to \code{"long"}.}
}
\value{
A list identical to the input \code{model_list}, but with an added element
\verb{$apa_mlm_report} in each model. Each \verb{$apa_mlm_report} contains one or more
\strong{flextable} objects formatted in APA style:
\describe{
\item{\verb{$summary}}{APA-style summary of the random-effects decision process.}
\item{\verb{$comparison_X}}{One or more detailed likelihood-ratio test tables
(if \code{verbosity = "long"}).}
}
}
\description{
Iterates through a list of model objects containing multilevel model (MLM)
comparison histories and produces APA-formatted summary tables for each.
Each table is generated using \code{\link[=apa_mlm_report]{apa_mlm_report()}}, displaying the model
comparison process (e.g., random-slope removal, covariance tests) in
a concise, publication-ready format.
}
\details{
The function is typically used after running \code{\link[=mlm_comparison]{mlm_comparison()}} and
constructing an \code{"mlm_report"} object with \code{\link[=make_mlm_report]{make_mlm_report()}}.
It automatically extracts the comparison history (e.g., \verb{$mc}) from each
element in \code{model_list}, builds a structured report with \code{\link[=make_mlm_report]{make_mlm_report()}},
and formats the output via \code{\link[=apa_mlm_report]{apa_mlm_report()}}.

Titles for each model are generated dynamically:
\itemize{
\item The \strong{bold_title} appears as the top row (commonly left blank or set
to \code{"Table"} for numbered tables).
\item The \strong{italics_title} is constructed as
\code{paste0(title_prefix, model_name)} (e.g.,
\emph{"Mixed Model Random-Effects Comparison: EP_PRCPSMSy"}).
}

The resulting APA-style tables are stored back into each model list element
under \verb{$apa_mlm_report}. These outputs can later be saved as images or DOCX
files using \code{\link[=save_apa_mlm_reports]{save_apa_mlm_reports()}}.
}
\examples{
\dontrun{
# Suppose each element in `models` contains an `$mc` object
# generated by mlm_comparison().

# Generate APA-formatted summaries for all models
models <- run_apa_mlm_report(
  model_list = models,
  mc_path = "mc",
  bold_title = "Table",
  title_prefix = "Random Effects Decision Process: ",
  font = "Garamond",
  font_size = 12,
  sig_level = .20,
  verbosity = "long"
)

# Access the formatted flextable for the first model
models[[1]]$apa_mlm_report$summary
}

}
\seealso{
\code{\link[=apa_mlm_report]{apa_mlm_report()}}, \code{\link[=make_mlm_report]{make_mlm_report()}}, \code{\link[=mlm_comparison]{mlm_comparison()}},
\code{\link[=save_apa_mlm_reports]{save_apa_mlm_reports()}}, \code{\link[flextable:flextable]{flextable::flextable()}}
}
