% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apa_helpers.R
\name{run_apa_mlm_report}
\alias{run_apa_mlm_report}
\title{Generate APA-Style MLM Reports for Multiple Model Comparisons}
\usage{
run_apa_mlm_report(
  model_list,
  mc_path = "mc",
  title_prefix = "Mixed Model Random-Effects Comparison: ",
  font = "Times New Roman",
  font_size = 11,
  sig_level = 0.2,
  verbosity = c("long", "short")
)
}
\arguments{
\item{model_list}{A named list of model specifications, each containing an
MLM comparison object (e.g., \verb{$mc}) generated by \code{\link[=mlm_comparison]{mlm_comparison()}}.}

\item{mc_path}{Character string indicating the nested path within each model
list element that points to the comparison history (e.g., \code{"mc"} or
\code{"results$mc"}). Defaults to \code{"mc"}.}

\item{title_prefix}{Character string prepended to the report title for each
model. Defaults to \code{"Mixed Model Random-Effects Comparison: "}.}

\item{font}{Character string specifying the font used for APA-style tables.
Defaults to \code{"Times New Roman"}.}

\item{font_size}{Numeric font size for the tables. Defaults to \code{11}.}

\item{sig_level}{Numeric significance threshold passed to
\code{\link[=apa_anova_comparison]{apa_anova_comparison()}} for bolding p-values. Defaults to \code{0.2}.}

\item{verbosity}{Character string indicating report detail level.
Must be \code{"long"} (summary + detailed ANOVA tables) or \code{"short"}
(summary only). Defaults to \code{"long"}.}
}
\value{
A list identical to the input \code{model_list}, but with an additional
element \verb{$apa_mlm_report} for each model.
Each \verb{$apa_mlm_report} contains one or more \strong{flextable} objects formatted
according to APA reporting conventions.
}
\description{
Iterates through a list of fitted model specifications and automatically
generates APA-style mixed-model comparison reports for each.
This function serves as the final step in the multilevel modeling pipeline,
producing formatted \strong{flextable} summaries and likelihood ratio test tables
via \code{\link[=apa_mlm_report]{apa_mlm_report()}}.
}
\details{
Each element of \code{model_list} should contain an existing multilevel model
comparison history object (typically stored under the \verb{$mc} element created
by \code{\link[=mlm_comparison]{mlm_comparison()}} or \code{\link[=run_mlm_comparisons]{run_mlm_comparisons()}}).
The function retrieves this history dynamically using the \code{mc_path}
argument, constructs a structured report with \code{\link[=make_mlm_report]{make_mlm_report()}},
and formats it into APA-style tables using \code{\link[=apa_mlm_report]{apa_mlm_report()}}.

The resulting report(s) are stored back into the list under the new
element \verb{$apa_mlm_report}.
}
\examples{
\dontrun{
# Run stepwise model comparisons first
models <- run_mlm_comparisons(model_list)

# Generate APA-formatted reports
models <- run_apa_mlm_report(models, verbosity = "short")

# Access the first model's formatted report
models[[1]]$apa_mlm_report
}

}
\seealso{
\code{\link[=apa_mlm_report]{apa_mlm_report()}}, \code{\link[=make_mlm_report]{make_mlm_report()}}, \code{\link[=mlm_comparison]{mlm_comparison()}},
\code{\link[=run_mlm_comparisons]{run_mlm_comparisons()}}
}
