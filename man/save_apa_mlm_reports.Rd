% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apa_tbl_helpers.R
\name{save_apa_mlm_reports}
\alias{save_apa_mlm_reports}
\title{Save APA-Style MLM Reports to Image Files}
\usage{
save_apa_mlm_reports(
  model_list,
  directory,
  report_path = "apa_mlm_report",
  prefix = "apa_mlm_report",
  create_subfolder = TRUE
)
}
\arguments{
\item{model_list}{A named list of model specifications, each containing
an APA-style report object (typically \verb{$apa_mlm_report}).}

\item{directory}{Character string specifying the base directory where
images will be saved. Created recursively if it does not exist.}

\item{report_path}{Character string indicating the path to the APA report
within each model object (e.g., \code{"apa_mlm_report"}). Defaults to \code{"apa_mlm_report"}.}

\item{prefix}{Character string used as the filename prefix for all saved
image files and the default subfolder name. Defaults to \code{"apa_mlm_report"}.}

\item{create_subfolder}{Logical or character.
\itemize{
\item If \code{TRUE}, a subfolder named after \code{prefix} is created.
\item If \code{FALSE}, images are saved directly into \code{directory}.
\item If a character string is provided, it is used as a custom subfolder name.
}}
}
\value{
Invisibly returns the input \code{model_list} after saving all report tables
as \code{.png} images.
Files are written to disk; a message indicates the final output location.
}
\description{
Exports APA-style mixed-model report tables (created by \code{\link[=apa_mlm_report]{apa_mlm_report()}}
or \code{\link[=run_apa_mlm_report]{run_apa_mlm_report()}}) as \code{.png} image files.
Each table (summary or comparison) is saved to a specified output directory,
optionally within an automatically created subfolder.
}
\details{
This function loops through a list of model specifications that each contain
an APA-formatted report (stored under the element specified by \code{report_path}).
For each model, all \strong{flextable} objects are converted to high-resolution
\code{.png} images using \code{\link[flextable:save_as_image]{flextable::save_as_image()}}.

By default, images are saved to a subdirectory named after the prefix
(e.g., \code{"apa_mlm_report"}). You can disable subfolder creation or provide
a custom folder name using the \code{create_subfolder} argument.
}
\examples{
\dontrun{
# Generate APA reports
models <- run_apa_mlm_report(model_list)

# Save all reports as PNGs in a new subfolder
save_apa_mlm_reports(models, directory = "output/reports")
}

}
\seealso{
\code{\link[=apa_mlm_report]{apa_mlm_report()}}, \code{\link[=run_apa_mlm_report]{run_apa_mlm_report()}}, \code{\link[flextable:save_as_image]{flextable::save_as_image()}}
}
