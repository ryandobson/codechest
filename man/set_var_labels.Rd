% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/var_labels.R
\name{set_var_labels}
\alias{set_var_labels}
\title{Set human-readable variable labels}
\usage{
set_var_labels(df, ..., labels = NULL, warn_missing = TRUE)
}
\arguments{
\item{df}{A data frame.}

\item{...}{Labels as \code{column = "Label"} pairs.}

\item{labels}{Alternatively, a named character vector or list, which is
easier to build programmatically. Merged with \code{...} if both are given.}

\item{warn_missing}{Warn about names that are not columns of \code{df}.
Default \code{TRUE}.}
}
\value{
\code{df}, with labels attached.
}
\description{
Attaches a \code{"label"} attribute to one or more columns. Functions that plot
or tabulate can then use the label instead of the bare column name, so
"Dried plant weight (g)" appears on an axis without being typed at every
call site.
}
\details{
This is base R: the label is an ordinary attribute. \code{\link[=apply_variable_info]{apply_variable_info()}}
does the same job through \code{haven::labelled()}, which also carries value
labels for coded variables; prefer that when you need those, and this when
you only want a display name.
}
\section{What preserves a label}{

Labels survive dplyr verbs, \code{as_tibble()}, \code{rbind()}, \code{model.frame()},
arithmetic on the column, and \code{saveRDS()}.

They are \strong{dropped} by base R row subsetting (\code{df[i, ]} and \code{subset()}) and
by \code{c()}. The subsetting case is worth knowing, since it is the one that
looks safe. Either label after subsetting, or subset with dplyr.

A CSV cannot carry labels at all, so a write/read round trip loses them.
}

\examples{
d <- set_var_labels(PlantGrowth,
                    weight = "Dried plant weight (g)",
                    group  = "Treatment condition")
var_labels(d)

# or from a vector, which is easier to keep in one place
lab <- c(weight = "Dried plant weight (g)", group = "Treatment condition")
d2 <- set_var_labels(PlantGrowth, labels = lab)
identical(var_labels(d), var_labels(d2))

}
\seealso{
\code{\link[=var_labels]{var_labels()}} to read them back, \code{\link[=apply_variable_info]{apply_variable_info()}} for the
haven route with value labels.

Other labelling:
\code{\link[=var_labels]{var_labels()}}
}
\concept{labelling}
