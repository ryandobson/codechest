% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/var_labels.R
\name{var_labels}
\alias{var_labels}
\title{Read the variable labels off a data frame}
\usage{
var_labels(df, missing = c("name", "na"))
}
\arguments{
\item{df}{A data frame.}

\item{missing}{What to return for a column with no label: \code{"name"}
(default) gives the column name, which is what a plot would fall back to
anyway; \code{"na"} gives \code{NA_character_}, which is what you want when checking
which columns still need labelling.}
}
\value{
A named character vector, one element per column.
}
\description{
Read the variable labels off a data frame
}
\examples{
d <- set_var_labels(PlantGrowth, weight = "Dried plant weight (g)")
var_labels(d)

# which columns still need a label?
names(which(is.na(var_labels(d, missing = "na"))))

}
\seealso{
\code{\link[=set_var_labels]{set_var_labels()}}

Other labelling:
\code{\link[=set_var_labels]{set_var_labels()}}
}
\concept{labelling}
