% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lavaan_constraint_engine.R
\name{.parameter_label_engine}
\alias{.parameter_label_engine}
\title{Decide an item-level parameter's label}
\usage{
.parameter_label_engine(
  item,
  item_index,
  block_id = NULL,
  mode,
  factor_name = NULL,
  scale_id = c("none", "marker", "effects_coded"),
  marker_items = NULL,
  free_items = NULL,
  label_prefix = "p",
  explicit_labels_when_free = TRUE
)
}
\arguments{
\item{item}{The item (or, for covariances, \code{"lhs_rhs"} pair id) being
labelled.}

\item{item_index}{The item's 1-based position within its factor/vector;
used to build the default label (\code{paste0(label_prefix, item_index)}).}

\item{block_id}{The block's id (e.g. group number), or \code{NULL} outside a
multi-group/multilevel context. Only used when a parameter is freed
and \code{explicit_labels_when_free = TRUE}.}

\item{mode}{One of \code{"equal_all"}, \code{"equal_except"}, \code{"free_all"}, or
\code{"fixed_zero"}.}

\item{factor_name}{The factor the item belongs to; required to check
\code{marker_items}.}

\item{scale_id}{\code{"none"}, \code{"marker"}, or \code{"effects_coded"}.}

\item{marker_items}{A named list keyed by factor name, giving the marker
item(s) for that factor when \code{scale_id = "marker"}.}

\item{free_items}{Items to free under \code{mode = "equal_except"}.}

\item{label_prefix}{The label prefix (e.g. \code{"l"}, \code{"i"}, \code{"r"}).}

\item{explicit_labels_when_free}{If \code{TRUE} (default), a freed parameter
gets an explicit \verb{<label>_g<block_id>} label rather than \code{NA}, so it
stays uniquely identified per block.}
}
\value{
A list with \code{type} (one of \code{"marker"}, \code{"fixed_zero"}, \code{"fixed"},
\code{"group_free"}, \code{"free_na"}) and \code{label} (the label string, or \code{NULL}/
\code{NA} when not applicable).
}
\description{
The label engine behind loadings, intercepts, residuals, and item
covariances: given one item and a \code{mode}, decides whether it should be
a scale-identification marker, fixed to zero, given a shared ("fixed")
label, or freely estimated (optionally with an explicit per-block label
so it stays distinguishable across groups).
}
\keyword{internal}
