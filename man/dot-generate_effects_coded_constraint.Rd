% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lavaan_constraint_engine.R
\name{.generate_effects_coded_constraint}
\alias{.generate_effects_coded_constraint}
\title{Generate an effects-coding sum constraint}
\usage{
.generate_effects_coded_constraint(
  label_prefix,
  n_items,
  sum_target = c("n_items", "zero")
)
}
\arguments{
\item{label_prefix}{The label prefix used for the summed parameters
(e.g. \code{"l"} for loadings, \code{"i"} for intercepts).}

\item{n_items}{The number of items being summed.}

\item{sum_target}{\code{"n_items"} (loadings sum to the item count) or
\code{"zero"} (intercepts sum to zero).}
}
\value{
A string with a leading blank line and comment, ready to insert
via \code{\link[=.insert_global]{.insert_global()}}.
}
\description{
Builds the \verb{l1 + l2 + ... + ln == <target>} constraint line that
identifies a factor's scale via effects coding (as opposed to a marker
item or fixed variance).
}
\keyword{internal}
